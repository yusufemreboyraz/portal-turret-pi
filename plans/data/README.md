# Measurement data (Faz 3)

| File | Produced by |
|------|-------------|
| `latency.csv` | `python3 turret/measure_results.py --latency-trials 20` |
| `benchmark_run.json` | same script (every run) |

Plot: `python3 plans/tools/plot_latency.py` → `ReportLatex/fig_latency.png`

Paste console summary into `plans/results.md` tables.
